version: "3.8"
services:
  pgsql:
      image: postgres:14.5-alpine
      volumes:
        - ".mount/pgsql/data/:/var/lib/postgresql/data/"
      ports:
        - "5432:5432"
      environment:
        - POSTGRES_HOST_AUTH_METHOD=trust
  app:
    build: .
    volumes:
      - ".:/usr/local/app"
    depends_on:
      - postgres
